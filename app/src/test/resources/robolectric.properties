# Robolectric configuration for unit tests
# sdk=34 matches the compileSdk target
sdk=34

# Use JDBC (pure-Java) SQLite driver instead of the native binary.
# The native loader uses Guava's createTempDir() with POSIX permissions
# which throws UnsupportedOperationException on Windows. JDBC avoids this entirely.
sqliteMode=JDBC
